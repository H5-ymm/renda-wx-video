var checkText = function (index) {
  var text = ''
  if(index == 1) {
    text = '对方取消视频'
  } else if (index == 2) {
    text = '对方拒绝视频'
  }else if (index == 3) {
    text = '对方不接视频'
  } else if (index == 4) {
    text = '对方已接听'
  } else {
    text = '已视频通话'
  }
  return text
}
module.exports = {
  checkText: checkText
}