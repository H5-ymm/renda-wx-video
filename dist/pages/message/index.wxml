
	<view class="message-view shadow page">
		<view class="page_margin view-content">
			<scroll-view scroll-y="true" style="height:100%"  wx:if="{{list.length}}" bindscrolltolower="searchScrollLower">
				<block wx:for="{{list}}" wx:key="index">
					<view class="weui-flex start message-row" bindtap="viewMessage" data-wpyviewmessage-a="{{item}}">
						<view class="message-col-1">
							<image mode="scaleToFill" src="../../images/header.png" class="message-col-header" wx-if="{{!item.header_img&&usertype==1}}"/>
							<image mode="scaleToFill" src="{{item.header_img}}" class="message-col-header" wx-if="{{item.header_img}}"/>
							<image mode="scaleToFill" src="{{item.logo_url}}" class="message-col-header" wx:if="{{item.logo_url}}"/>
							<image mode="scaleToFill" src="https://a.rsd123.com/image/images/default1.png" class="message-col-header" wx:if="{{!item.logo_url&&usertype!=1}}"/>
							<view class="bage" wx:if="{{item.person_read==1||item.com_read==1}}"/>
						</view>
						<view class="weui-flex between-start wrap message-col-2 weui-flex__item page_bottom">
							<view>
								<view class="page__title">{{usertype==1?(item.user_name||'-'):item.com_name}}</view>
								<view class="message-col-small page__desc">{{item.job_name}}</view>
							</view>
							<view class="message-col-small message-col-time page__desc">{{item.view_time}}</view>
						</view>
					</view>
				</block>
			</scroll-view>
			<view  wx:else class="page_list">
				<image src="http://www.ttxsg.com.cn:39009/uploads/images/noData.png" class="page_noData" mode="scaleToFill"/>
				<view class="page_noData_text">暂无消息！</view>
		   </view>
		</view>
	</view>